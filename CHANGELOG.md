# Changelog

이 프로젝트의 주요 변경 사항을 기록합니다.

## [0.0.4]

### 보안

- DB·SSH 비밀번호를 평문 `globalState` 대신 VS Code SecretStorage(암호화)에 저장하고, 기존 평문 저장분을 자동 마이그레이션.
- 모든 웹뷰에 Content-Security-Policy를 도입하고, 결과 데이터·식별자·컬럼명·파일명을 이스케이프해 스크립트/SQL 주입 표면 차단.
- SQL 식별자 백틱 이스케이프 및 컬럼 화이트리스트 검증 추가.
- SSH 개인키를 파일 내용으로 올바르게 로드하고 passphrase 입력을 지원.
- SQL 파일 이름 경로 순회 방지.

### 안정성

- 잘못된 편집으로 다른 행이 덮어써지던 문제(기본키 편집·NULL 처리·중복 행) 수정.
- UPDATE/DELETE가 0행에 매칭되면 오류로 알려 '가짜 삭제'를 방지.
- BIGINT 정밀도 손실과 날짜 시간대 밀림(dateStrings) 해결, Buffer/JSON 컬럼 표시·편집 처리.
- SSH 터널 에러 핸들링·keepalive·끊김 감지, 커넥션 유휴 복구 재시도, 연결 검증, 경쟁 조건·리소스 정리 개선.

### 기능

- 다중 SQL 문장 실행과 커서 위치 문장 실행, 대용량 SELECT 자동 LIMIT.
- SQL 파일 실행 결과를 그리드로 표시.
- 빈 상태 안내, 커맨드 팔레트 정리, 단축키 범위 제한, CSV 수식 주입 방어.
- 페이지 크기·기본 LIMIT·최대 삭제 행 수 사용자 설정 추가.
- 쿼리 자동완성 추가: 관리 SQL 파일에서 database·table·column 제안(`.` 트리거, JOIN alias 및 한정 식별자 인식). 메타데이터 TTL 캐시와 Refresh 시 무효화 적용.

### 빌드

- esbuild 번들링으로 전환(네이티브 바이너리 미포함, 크로스플랫폼), 패키지 위생 개선.

## [0.0.2]

- 초기 공개 버전.
